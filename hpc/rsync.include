# Use this file by running rsync from the project's root directory with:
# rsync -av --delete --include-from=hpc/rsync.include . csd3:thermo
# Test by adding the --dry-run option.

# Copy results back from hpc with
# rsync -av csd3:thermo/results ~/Desktop
# Note: requires setting up csd3 as an ssh alias for jr769@login.hpc.cam.ac.uk.

- __pycache__
- .DS_Store
- results
- logs